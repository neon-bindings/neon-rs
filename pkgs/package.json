{
  "name": "pkgs",
  "private": true,
  "version": "0.0.197",
  "description": "workspace root for all publishable @neon-rs and related packages",
  "workspaces": [
    "install",
    "load",
    "cli",
    "cargo-messages"
  ],
  "scripts": {
    "version": "node ./cli bump -v --workspaces"
  }
}
