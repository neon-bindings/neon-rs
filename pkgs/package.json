{
  "name": "pkgs",
  "private": true,
  "version": "0.0.121",
  "description": "workspace root for all publishable @neon-rs and related packages",
  "workspaces": [
    "load",
    "cli",
    "cargo-messages"
  ],
  "scripts": {
    "version": "cross-os version",
    "version:unix": "npm version $npm_package_version --workspaces && (cd src/cli && npm version $npm_package_version) && git add .",
    "version:windows": "npm version %npm_package_version% --workspaces && cd src && cd cli && npm version %npm_package_version% && cd .. && cd .. && git add ."
  },
  "cross-os": {
    "version": {
      "darwin": "npm run version:unix",
      "linux": "npm run version:unix",
      "win32": "npm run version:windows"
    }
  }
}
