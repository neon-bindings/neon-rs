{
  "name": "pkgs",
  "private": true,
  "version": "0.0.187",
  "description": "workspace root for all publishable @neon-rs and related packages",
  "workspaces": [
    "install",
    "load",
    "cli",
    "cargo-messages"
  ],
  "scripts": {
    "help": "node ./cli help",
    "which": "echo $(which sh) && echo $(which neon) && echo $(which npm)",
    "version": "neon bump -v --workspaces"
  },
  "devDependencies": {
    "@neon-rs/cli": "^0.0.186"
  }
}
