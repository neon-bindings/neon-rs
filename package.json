{
  "name": "neon-rs",
  "private": true,
  "version": "0.2.3",
  "description": "@neon-rs monorepo root",
  "workspaces": [
    "pkgs/artifact",
    "pkgs/cargo-messages",
    "pkgs/load",
    "pkgs/manifest",
    "pkgs/cli"
  ],
  "scripts": {
    "version": "npm version --no-git-tag-version --workspaces $npm_package_version && npm i && git add .",
    "release": "gh workflow run release.yml -f dryrun=false -f version=patch",
    "dryrun": "gh workflow run release.yml -f dryrun=true"
  }
}
