{
  "name": "neon-rs",
  "private": true,
  "version": "0.0.114",
  "description": "@neon-rs workspace root",
  "workspaces": [
    "pkgs/cargo-messages",
    "pkgs/cli",
    "pkgs/cli-dev",
    "pkgs/load"
  ],
  "scripts": {
    "version": "npm version $npm_package_version --workspaces && git add **/package.json **/package-lock.json",
    "release": "npm version patch -m 'v%s' && git push --follow-tags"
  }
}
