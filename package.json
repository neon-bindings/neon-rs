{
  "name": "neon-rs",
  "private": true,
  "version": "0.0.191",
  "description": "@neon-rs monorepo root",
  "workspaces": [],
  "scripts": {
    "postinstall": "node pkgs/install src --workspaces --omit=optional && node pkgs/install pkgs --workspaces",
    "update-src": "cd src && cd cli && npm run update-binaries && npm run update-cargo-messages && npm run bundle",
    "version": "npm run version-src && npm run version-pkgs && git add .",
    "version-src": "node pkgs/cli bump -v --dir src",
    "version-pkgs": "node pkgs/cli bump -v --dir pkgs",
    "release": "npm version -m 'v%s'",
    "postrelease": "git push --follow-tags",
    "dryrun": "gh workflow run publish.yml -f dryrun=true"
  }
}
