{
  "name": "neon-rs",
  "private": true,
  "version": "0.2.2",
  "description": "@neon-rs monorepo root",
  "workspaces": [
    "pkgs/*"
  ],
  "scripts": {
    "dist": "npm run dist:artifact && npm run dist:manifest",
    "dist:artifact": "cd pkgs/artifact && npm run dist",
    "dist:manifest": "cd pkgs/manifest && npm run dist",
    "version": "npm version --no-git-tag-version --workspaces $npm_package_version && npm run dist && npm i && git add .",
    "release": "gh workflow run release.yml -f dryrun=false -f version=patch",
    "dryrun": "gh workflow run release.yml -f dryrun=true"
  }
}
